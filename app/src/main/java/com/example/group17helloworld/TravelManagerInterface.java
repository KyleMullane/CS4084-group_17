package com.example.group17helloworld;

public interface TravelManagerInterface {
       /* void login(String username, String password); //maybe return a user object
        void signup(); //maybe returns and saves a new user object
        void logout();
        void createTrip();
        ArrayList<Trip> getUpcomingTrips(); //when you hit the button of upcoming trips
        ArrayList<Trip> getCurrentTrips(); //when you hit the button of current trips
        ArrayList<Trip> getPastTrips(); //when you hit the button of past trips
        Trip getTripDetails(Trip trip); //idk what this returns //if we want to display trip info on one screen return all 3 types??
        void createAccommodation();
        void createActivity();   //do we need parameters?? (I think so based on user input), use another class to save it to database?? --> save method is called in these methods
        void createFlight();
        ArrayList<Accommodation> accessTripAccommodations();
        ArrayList<Activity> accessTripActivities();  //these 3 methods are used when you press the button to go to the page for each category
        ArrayList<Flight> accessTripFlights();
        Accommodation viewAccommodation();
        Activity viewActivity();    //we will have a class that deals with formatting the information accessed by these methods
        Flight viewFlight();
        void cancelTrip();
        void cancelAccommodation();
        void cancelActivity();  //deletes these from the database
        void cancelFlight();
        void deleteUser();
        void updateAccommodation(Accommodation accommodation);
        void updateActivity(Activity activity);   //maybe need more specific methods depending on what details you want to update
        void updateFlight(Flight flight);
        void reviewTrip();
        void shareTripWithPublic(Trip trip); //might need a new entity set for shared trips so all trips to be displayed to public are in one place
        ArrayList<Trip> getSuggestedTripsLocation(String location);
        ArrayList<Trip> getSuggestedTripsRatings(Integer rating); */

    }
